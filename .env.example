# Z.AI API Keys
# NOTE: For maximum speed, the code attempts the ZAI_FALLBACK_KEY (open.bigmodel.cn) FIRST. 
# If it fails or is not provided, it falls back to the primary ZAI_API_KEY (api.z.ai).
ZAI_API_KEY=your_zai_api_key_here
ZAI_FALLBACK_KEY=your_zhipu_open_bigmodel_key_here
